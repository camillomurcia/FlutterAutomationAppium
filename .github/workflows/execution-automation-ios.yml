name: Ejecucion de pruebas en iOS

#on:
#  workflow_run:
#    workflows: ["Compilacion"]
#    types:
#      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    name: "Ejecucion de pruebas iOS"
    steps:
      - uses: actions/checkout@v2
      - name: Ejecucion de pruebas iOS
        run: |
          chmod +x gradlew
          ./gradlew clean test -Dappium.hub="${{ secrets.KOBITON }}" -Dproperties="serenityiOS.properties" -i